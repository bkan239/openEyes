# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
out/
dist/
build/
*.tsbuildinfo

# SST / Pulumi
.sst/
.open-next/
.vercel/

# Lambda build artifacts
services/api/.lambda-build/
services/api/.lambda-package.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env & secrets
.env
.env.*
.env*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code — personal settings only (shared agents/skills ARE committed)
.claude/settings.local.json
.claude/*.local.json

# Skills CLI artifacts (we vendor skills under .claude/skills instead)
.agents/
skills-lock.json

# Local media test fixtures (uploaded clips)
/tmp-media/
~$OpenEyes-Pitch-Deck (16).pptx

# OpenEyes video (Remotion) — keep source + media deliverables, ignore build
video/node_modules/
video/out/
video/public/scene.glb
video/public/clips
video/public/thumbs
# (media/*.mp4 etc. are committed deliverables — do NOT ignore)
