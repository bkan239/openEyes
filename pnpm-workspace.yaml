# pnpm/TS workspaces. The other apps are intentionally independent:
#   - services/api  Python (uv)
#   - iOSApp        native SwiftUI (Xcode)
#   - angles        Vite app with its own npm/package-lock
packages:
  - "packages/*"
